import 'package:shared_preferences/shared_preferences.dart';

import '../common/constants.dart';

mixin LoginOperationsMixin {

  // static String storedEmail = "";
  // static String storedPass = "";
  //
  // void loadPrefs(SharedPreferences prefs) async {
  //   storedEmail = prefs.getString(kStoredEmailTag) ?? "";
  //   storedPass = prefs.getString(kStoredPassTag) ?? "";
  // }
  //
  // void saveUserToPrefs(SharedPreferences prefs, String newEmail, String newPass) async {
  //   prefs.setString(kStoredEmailTag, newEmail);
  //   prefs.setString(kStoredPassTag, newPass);
  // }
}
